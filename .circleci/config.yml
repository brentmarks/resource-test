version: 2.1

workflows:
  main:
    jobs:
      - test

jobs:
  test:
    machine:
      image: ubuntu-2004:202101-01
    resource_class: arm-medium
    steps:
      - checkout:
          path: project
